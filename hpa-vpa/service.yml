kind: Service
apiVersion: v1
metadata:
   name: hpa-service
   namespace: apache
spec:
  selector:
      app: apache-app
  ports:
   - protocol: TCP
     port: 80
     targetPort: 80
  type: ClusterIP

