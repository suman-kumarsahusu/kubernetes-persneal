kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
   name: nginx-ingress
   namespace: nginx
   annotations:
     nginx.ingress.kubernetes.io/rewrite-target: /
spec:
   rules:
     - http:  
        paths:
        - pathType: Prefix
          path: /nginx
          backend:
            service:
              name: service-demo
              port: 
               number: 80
        - pathType: Prefix
          path: /
          backend: 
           service:
             name: ingress-svc 
             port: 
              number: 8000

